import {useState} from 'react';import{Link,NavLink}from'react-router-dom';import logo from '../assets/known-logo-gold.png';
export default function Header(){const[open,setOpen]=useState(false);const links=[['Story','/story'],['Summit','/summit'],['Community','/community'],['Attend','/attend'],['Build','/partnerships']];return <header className="site-header"><div className="shell nav"><Link className="brand" to="/"><img src={logo}/><span>THE KNOWN MOVEMENT</span></Link><button className="menu" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?'Close':'Menu'}</button><nav className={open?'open':''}>{links.map(([l,p])=><NavLink onClick={()=>setOpen(false)} key={p} to={p}>{l}</NavLink>)}<NavLink className="nav-cta" to="/attend">Apply</NavLink></nav></div></header>}
